# Local platform store

JSON files written here (`events.json`, `leads.json`, `observations.json`, `keys.json`, `meter.json`) stay on this machine.

They are gitignored. There is no hosted database, no partner directory, and no billing ledger.

`PUBLIC_SITE_LIVE` remaining `false` is independent of this folder.
